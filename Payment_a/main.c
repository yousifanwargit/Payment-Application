#include <stdio.h>
#include "app.h"
/*
ST_cardData_t card1 = {
	0
};

ST_terminalData_t terminal1 = {
	0
};

ST_transaction_t transaction1 = { 0 };


uint8_t app_server_error = 0;*/


int main() {
	/*
	init_accountDB();

	//getCardHolderName(&card1);
	//getCardExpiryDate(&card1);
	getCardPAN(&card1);

	//getTransactionDate(&terminal1);
	//isCardExpired(card1, terminal1);
	setMaxAmount(&terminal1);
	getTransactionAmount(&terminal1);
	isBelowMaxAmount(&terminal1);
	//app_server_error = isValidAccount(&card1);
	//app_server_error = isAmountAvailable(&terminal1);
	//transaction1.transState = app_server_error;
	//saveTransaction(&transaction1);
	recieveTransactionData(&transaction1);
	*/

	appStart();
	return 0;
}